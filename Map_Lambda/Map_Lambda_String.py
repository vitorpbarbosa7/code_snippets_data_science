# map para aplicar a função lambda a todos componentes de w
# lambda para criar a função que será aplicada todas vezes a todos componentes de w

print("Coeficientes finais padronizados: " + ', '.join(map(lambda x: "%0.4f" % x, w)))